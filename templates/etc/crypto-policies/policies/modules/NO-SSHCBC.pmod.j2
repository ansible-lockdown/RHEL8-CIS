# Added as part of RHEL8-CIS by ansible-lockdown
# Sponsored by Mindpoint Group - A Tyto Athene Company / Ansible Lockdown

# This is a subpolicy to disable all CBC mode ciphers
# for the SSH protocol (libssh and OpenSSH)
cipher@SSH = -*-CBC
