---
# Post tasks
---

- name: "SCORED | POST | PATCH | Ensure updates, patches, and additional security software are installed"
  package:
      name: "*"
      state: latest
  ignore_errors: yes
    
- name: Perform DNF package cleanup
  dnf: 
    autoremove: yes
  changed_when: no
  ignore_errors: yes
